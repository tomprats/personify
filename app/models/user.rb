class User < ActiveRecord::Base
  # From http://stackoverflow.com/a/2357790/1197246
  def age
    now = Time.now.utc.to_date
    now.year - birthday.year - ((now.month > birthday.month || (now.month == birthday.month && now.day >= birthday.day)) ? 0 : 1)
  end
end
